interface:
  display_name: "Wire up this environment"
  short_description: "Follow the setup procedure for this kind of environment, owned machine or devspaces pod"
policy:
  allow_implicit_invocation: false
